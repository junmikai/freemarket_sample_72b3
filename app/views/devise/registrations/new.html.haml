
.warapper
  %h2.warapper__log 会員情報登録

  .feilds
    = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
      = devise_error_messages!

      %hr/
      .field
        = f.label :ニックネーム
        %span.form_require
          必須
        %br/
        = f.text_field :nickname, placeholder: "例) メルカリ太郎"

      .field
        = f.label :メールアドレス
        %span.form_require
          必須
          %br/
        = f.email_field :email, autofocus: true, autocomplete: "email",placeholder:"PC・携帯どちらでも可"

      .field
        = f.label :パスワード
        - if @minimum_password_length
          %em
            (#{@minimum_password_length} characters minimum)
        %br/
        = f.password_field :password, placeholder: "7文字以上の半角英数字"
        %br/
        ※ 英字と数字の両方を含めて設定してください
        %br/
        %br/
        %br/
        %label
          お名前(全角)
        %span.form_require
          必須
        %br/
        .fieldname
          .form_flex
            = f.text_field :family_name, placeholder: "例) 山田"
            = f.text_field :first_name, placeholder: "例) 彩"
        

        %label
          お名前カナ(全角)
        %span.form_require
          必須
        %br/
        .fieldname
          .form_flex
            = f.text_field :family_name_kana, placeholder: "例) ヤマダ"
            = f.text_field :first_name_kana, placeholder: "例) アヤ"
        
      
        .field
          = f.label :birth_date
          %br/
          = f.date_select :birth_day, use_month_numbers: true,start_year: 1930, end_year: (Time.now.year - 10), default: Date.new(1989, 1, 1)

        .actions
          = f.submit "フリマを始める", class: "btn"
